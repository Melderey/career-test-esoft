const PORT = process.env.PORT || 5000;

const MAIN_ROUTE = "/main";
const GUEST_ROUTE = "/guest_chat";
const OPERATOR_ROUTE = "/operator_chat";
const LOGIN_ROUTE = "/login";

module.exports = {
  PORT,
  MAIN_ROUTE,
  GUEST_ROUTE,
  OPERATOR_ROUTE,
  LOGIN_ROUTE,
};
